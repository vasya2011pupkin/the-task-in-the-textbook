#include "stdafx.h"
#include "conio.h"
#include <iostream>
#include <limits.h>

int main()
{
	setlocale(LC_ALL, "Russian");
	
	int a, b, c, d, e, x = 0, y = INT_MAX;

   printf ("Введите пять целых чисел: \n");
   scanf ("%d%d%d%d%d", &a, &b, &c, &d, &e);

   if (a > x)
	   x = a;
   if (b > x)
	   x = b;
   if (c > x)
	   x = c;
   if (d > x)
	   x = d;
   if (e > x)
	   x = e;

   if (a < y)
	   y = a;
   if (b < y)
	   y = b;
   if (c < y)
	   y = c;
   if (d < y)
	   y = d;
   if (e < y)
	   y = e;

   printf ("Большее число: %d\n", x);
   printf ("Меньшее число: %d\n", y);

	_getch ();
	return 0;
}
